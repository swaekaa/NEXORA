"""
NEXORA — Merchant Agent Configuration

NOTE: This module is intentionally minimal.
The Merchant Agent uses the shared LLM factory from `app.llm`.
Import `get_llm` directly from `app.llm` in nodes.py.

This file is kept for package structure consistency only.
"""
