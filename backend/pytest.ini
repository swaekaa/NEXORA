[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show local variables on failure for easier debugging
log_cli = true
log_cli_level = WARNING

# Coverage (when run with --cov)
addopts = -v --tb=short
